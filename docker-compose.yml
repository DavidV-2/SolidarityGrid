
services:
  # SQL Server
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: solidaritygrid-sql
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=YourStrong!Passw0rd123
      - MSSQL_PID=Express
    ports:
      - "1433:1433"
    volumes:
      - sqlserver-data:/var/opt/mssql
    networks:
      - solidaritygrid-network

  # Nodo A
  node-a:
    build:
      context: .
      dockerfile: src/SolidarityGrid.Api/Dockerfile
    container_name: solidaritygrid-node-a
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:8080
      - ConnectionStrings__DefaultConnection=Server=sqlserver;Database=SolidarityGrid;User Id=sa;Password=YourStrong!Passw0rd123;TrustServerCertificate=true;Encrypt=false
      - Node__NodeName=node-a
      - Node__PeerNodes__0=http://node-b:8080
      - Node__PeerNodes__1=http://node-c:8080
    ports:
      - "8081:8080"
    depends_on:
      - sqlserver
    networks:
      - solidaritygrid-network
    restart: unless-stopped

  # Nodo B
  node-b:
    build:
      context: .
      dockerfile: src/SolidarityGrid.Api/Dockerfile
    container_name: solidaritygrid-node-b
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:8080
      - ConnectionStrings__DefaultConnection=Server=sqlserver;Database=SolidarityGrid;User Id=sa;Password=YourStrong!Passw0rd123;TrustServerCertificate=true;Encrypt=false
      - Node__NodeName=node-b
      - Node__PeerNodes__0=http://node-a:8080
      - Node__PeerNodes__1=http://node-c:8080
    ports:
      - "8082:8080"
    depends_on:
      - sqlserver
    networks:
      - solidaritygrid-network
    restart: unless-stopped

  # Nodo C
  node-c:
    build:
      context: .
      dockerfile: src/SolidarityGrid.Api/Dockerfile
    container_name: solidaritygrid-node-c
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:8080
      - ConnectionStrings__DefaultConnection=Server=sqlserver;Database=SolidarityGrid;User Id=sa;Password=YourStrong!Passw0rd123;TrustServerCertificate=true;Encrypt=false
      - Node__NodeName=node-c
      - Node__PeerNodes__0=http://node-a:8080
      - Node__PeerNodes__1=http://node-b:8080
    ports:
      - "8083:8080"
    depends_on:
      - sqlserver
    networks:
      - solidaritygrid-network
    restart: unless-stopped

networks:
  solidaritygrid-network:
    driver: bridge

volumes:
  sqlserver-data: